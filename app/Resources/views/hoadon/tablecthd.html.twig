<table class="table table-hover" id="tb" >
        <thead>
            <tr>
                <th>Sản Phẩm</th>
                <th>Số lượng</th>
                <th>Đơn giá</th>
                <th>Thành tiền</th>
                <th>Action</th>
            </tr>
        </thead>
        {% set sum = 0 %}
        {%  set  vt = 0 %}
        {% for cthd in cthds %}
        <tbody>
            <td>{{cthd.tensp}}</td>
            <td>{{cthd.sl}}</td>
            <td>{{cthd.dongia}}</td>
            <td>{{cthd.thanhtien}}</td>
               {% set sum = sum + cthd.thanhtien %} 
            <td><a class="deleteCTHD btn btn-danger" data-playgroup-id ="{{ vt }}" >Delete</a></td>
            {% set vt = vt + 1 %} 
        </tbody>
        {% endfor %}
        <tbody>
            <td></td>
            <td></td>
            <td>Tổng tiền:</td>
            <td>{{ sum }}</td>

        </tbody>
            
</table>


